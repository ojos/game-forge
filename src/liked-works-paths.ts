/**
 * 「自分がいいねした作品」の経路のパス（5.8 / 2.3.1 / M9-8 / #340）。
 *
 * # なぜ `src/paths.ts` に置かないのか
 *
 * **`src/paths.ts` はオーケストレータ Lambda の束に入る**（#336）。束に残った値が 1 つ
 * 増えるだけで Lambda の `CodeSha256` が変わり、#241 の関門が本番配備を止める
 * （**同じ形の停止を 3 回踏んでいる**。`src/works-paths.ts` の冒頭）。**いいねの一覧の
 * 綴りは Lambda が一度も読まない**ので、Lambda が import しないこの葉へ置く。
 *
 * # なぜ `src/works-paths.ts` にも置かないのか
 *
 * **あちらは M9 の並列作業で複数のレーンが読んでいる**（`src/works-list.ts` /
 * `src/my-works.ts` / トップ）。**行を足すだけで衝突する場所を増やさない**
 * （`docs/handoff.md` 4 章「中身を読み書きし合うファイルは例外にせず同じレーンへ畳む」）。
 * 綴りを足す側が自分の葉を持てば、取り込む側が 1 ファイルも突き合わせずに済む。
 *
 * # なぜ `src/liked-works.ts` に置かないのか
 *
 * **画面を提供する側（`src/liked-works.ts`）と、そこへ送り返す側（`src/my-works.ts`）が
 * 別モジュールになる。** 導線は「あなたの作品」の画面に置いた（#340）。**v1.57 で
 * ヘッダのアカウントのメニューにも入った**（2.3.7。#372）ので、外枠（`src/html.ts`）も
 * この綴りを借りる——外枠は画面のモジュールから借りられないので、ここが葉であることが
 * なおさら要る。値だけの葉へ置かないと、画面どうしが互いを import し合う形になる
 * （`src/works-paths.ts` が `PUBLIC_WORKS_PATH` と `MY_WORKS_PATH` について書いているのと
 * 同じ事情である）。
 *
 * **値はリテラルにする**（`src/like-paths.ts` と同じ規約）。この葉が将来どこかの束へ
 * 入っても、使われない値は落とせる形に保つ——**別の定数を参照する式にした 2 つが、
 * 実際に束へ残って配備を止めた**（#328。`src/works-paths.ts` の冒頭）。綴りが
 * 作品ページの接頭辞の下にあることは `test/liked-works.test.ts` が機械で照合する
 * （**関係を式ではなく検査で持つ**）。
 */

/**
 * 「自分がいいねした作品」（5.8 / 2.3.1。M9-8 で新設）。
 *
 * **本人だけが見られる。** 誰がいいねしたかは公開しないと決めており（5.8）、この一覧が
 * その唯一の出口である。未ログインならログインへ送る（`src/liked-works.ts`）。
 *
 * **完全一致で登録する。** `src/routes.ts` は完全一致を前方一致より先に見ると定めており、
 * 作品ページの前方一致（`/works/`）に飲み込まれない（`MY_WORKS_PATH` と同じ事情で、
 * その想定は `/works/mine` で既に 1 度実地に来ている）。
 */
export const LIKED_WORKS_PATH = '/works/liked';
