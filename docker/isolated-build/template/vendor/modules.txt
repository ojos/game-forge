# vendor 済み依存の一覧。M0.5-3 のサンプルは標準ライブラリだけを使うため空である。
#
# 空でも置いておく理由: このファイルが /src から /work へ複製されることが、
# 7.1 の前提 1（vendor を /work に置くと --tmpfs /work に隠される）が実際に
# 回避できていることの確認になる。Ebitengine の vendor 焼き込みは M2-4 が行う。
