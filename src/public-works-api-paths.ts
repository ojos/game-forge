/**
 * 公開作品の一覧を機械が読める口の綴り（#699 / M19-2。仕様 5.13）。
 *
 * **値だけの葉に置く**（`src/works-api-paths.ts` と同じ形）。MCP（#696）やチャットの道具がこの口を
 * 指すときに、口の本体（一覧のモジュールを読む）を連れてこないためである。
 */

/** 公開作品の一覧（`GET`）。**`/works` の画面と同じ引数を読む**（`sort` / `tag` / `q` / `page`）。 */
export const PUBLIC_WORKS_API_PATH = '/api/works';
