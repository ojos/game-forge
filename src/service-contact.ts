/**
 * 運営者の名称と、一般の問い合わせ窓口（2.3.7 v1.57 / #373）。
 *
 * ## なぜ 1 か所に集めるのか
 *
 * **同じ値が 3 か所に出る**——フッタのお問い合わせ区画（`src/legal.ts` の
 * `FOOTER_CONTACT_ITEMS`）、プライバシーポリシーの窓口（`src/privacy.ts`）、よくある質問
 * （`src/faq.ts`）。**画面ごとに書くと、アドレスを変えた日に 1 か所だけ古い宛先を
 * 指し続ける**——古い宛先へ送られた開示請求は、誰にも読まれない。
 *
 * ## なぜ値だけの葉に置くのか
 *
 * `src/legal-paths.ts` と同じ理由である。**外枠（`src/html.ts`）や画面のモジュールから
 * 借りられても循環参照にならない**よう、import を 1 つも持たない。
 *
 * ## 窓口はメールアドレスであり、画面ではない（#373 の scope.out）
 *
 * **問い合わせをフォームで受けて D1 へ保存しない。** 保存すると、個人情報として管理する
 * 対象が 1 つ増える。
 *
 * **`/takedown` と混ぜないこと。** あちらは権利者向けの窓口で、ここは一般の利用者の
 * 不具合報告・質問・開示等の請求の宛先である（2.3.7 v1.57 の注記）。
 */

/** 一般の問い合わせ窓口のメールアドレス（2026-09-12 に利用者が決めた）。 */
export const CONTACT_EMAIL = 'game-forge@ojos.jp';

/** 窓口へのリンク（`mailto:`）。フッタとプライバシーポリシーが共有する。 */
export const CONTACT_MAILTO = `mailto:${CONTACT_EMAIL}`;

/**
 * 運営者の名称（プライバシーポリシーの「事業者の名称」）。
 *
 * **暫定である。正式な名称へ差し替えが必要（差し替えるのはこの 1 行）。** クローズドβの
 * あいだの暫定として、利用者がサービス名での表記を選んだ（2026-09-12）。**個人情報保護法の
 * 公表事項（事業者の氏名または名称・住所・代表者）は、これだけでは満たさない。**
 * 住所と代表者はまだ書いていない——決まっていない値を作り話で埋めないため。
 */
export const OPERATOR_NAME = 'Game Forge 運営';
