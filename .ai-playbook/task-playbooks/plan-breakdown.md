# Plan Breakdown Task Skill

## 目的

- 要件を依存順の実行タスクへ分解し、実装着手可能な計画にする。

## 入力

- goal、scope、acceptance、priority。

## 手順

1. goal に対する完了条件を列挙する。
2. タスクを 1 つの責務ごとに分割する。
3. 依存関係を前提タスクとして明示する。
4. 並列実行可能タスクを識別する。

## 出力

- タスク一覧、依存順、担当候補、検証観点。

## 注意事項

- acceptance に紐づかない作業を計画へ入れない。
- 実装前に検証方法を最低1つ定義する。検証は**非対話で実行でき、合否が機械判定できる形**にする（[loop-workflow](../loop-workflow.md) の「前提: 完了条件は機械が検証できる形にする」に従う）。
